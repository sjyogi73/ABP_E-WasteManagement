
using System.Threading.Tasks;
using Shouldly;
using Xunit;

namespace eWasteManagement.Pages.UserDetail
{
    public class Index_Tests : eWasteManagementWebTestBase
    {
        /*
        [Fact]
        public async Task Index_Page_Test()
        {
            // Arrange

            // Act
            var response = await GetResponseAsStringAsync("/userDetail");

            // Assert
            response.ShouldNotBeNull();
        }
        */
    }
}
